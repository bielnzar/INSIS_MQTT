# credentials_plaintext.yaml
# PERINGATAN: Menyimpan password dalam teks biasa sangat tidak aman!

credentials:
  usernames:
    # Ganti dengan username dan password teks biasa Anda
    NabielGanteng: "1234" # CONTOH PASSWORD TEKS BIASA
    # userlain: "passwordlain456"
# Bagian cookie masih diperlukan jika Anda ingin menggunakan session state Streamlit
# untuk mengingat status login antar interaksi halaman, meskipun tidak seaman cookie
# yang di-manage oleh streamlit-authenticator. Untuk contoh ini, kita akan
# menggunakan session state Streamlit yang lebih sederhana.
# Jika Anda tidak butuh session management antar refresh, bagian cookie bisa diabaikan
# dan status login hanya ada selama script berjalan.

# Untuk contoh ini, kita akan sederhanakan dan tidak menggunakan cookie management
# dari streamlit-authenticator. Status login akan di-handle via st.session_state.
